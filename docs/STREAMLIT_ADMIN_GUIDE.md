# Streamlit 管理界面使用指南

## 目录
1. [快速开始](#快速开始)
2. [功能模块](#功能模块)
3. [配置说明](#配置说明)
4. [页面详解](#页面详解)
5. [故障排除](#故障排除)

## 快速开始

### 环境要求

- Python 3.11+
- FastAPI 后端服务已启动
- 已配置环境变量

### 安装依赖

```bash
pip install streamlit plotly pandas python-dotenv httpx tenacity structlog
```

### 配置环境变量

在项目根目录的 `.env` 文件中添加：

```env
# API 配置（必需）
API_BASE_URL=http://localhost:8000
API_KEY=your-api-key-here

# 管理界面配置（可选）
ADMIN_HOST=0.0.0.0
ADMIN_PORT=8501
ENV=development
```

### 启动管理界面

```bash
# 方式 1：使用启动脚本（推荐）
./scripts/start_admin.sh

# 方式 2：直接运行
streamlit run backend/admin/app.py

# 方式 3：指定端口
streamlit run backend/admin/app.py --server.port 8501
```

### 访问界面

浏览器访问：
```
http://localhost:8501
```

## 功能模块

### 1. 📊 统计仪表板

**功能**：
- 订单总览（总数、待支付、已支付、已交付等）
- 订单状态分布（饼图）
- 订单类型分布（柱状图）
- 订单流转漏斗图
- 成功率和支付率计算

**适用场景**：
- 快速了解业务概况
- 监控订单转化率
- 分析订单分布

### 2. 📦 订单管理

**功能**：
- 订单列表展示（支持分页）
- 多条件过滤（类型、状态）
- 订单详情查看
- 订单状态更新
- 订单取消操作

**操作流程**：
1. 选择过滤条件（订单类型、状态）
2. 浏览订单列表
3. 输入订单 ID 查看详情
4. 更新订单状态或取消订单

### 3. ⚙️ 系统设置

**功能**：
- API 配置管理
- 环境变量查看
- 系统信息展示
- 关于信息

**Tab 页签**：
- 🔑 API 配置：查看/复制 API Key
- 🌍 环境信息：查看所有环境变量
- ℹ️ 关于：版本信息、技术栈

### 4. 🏥 健康监控

**功能**：
- 整体服务健康状态
- 数据库健康检查
- Redis 健康检查
- Worker 健康检查
- 自动刷新（5/10/30/60 秒）

**状态说明**：
- 🟢 健康：所有组件正常
- 🟡 降级：部分组件异常
- 🔴 异常：关键组件异常

## 配置说明

### 必需环境变量

| 变量 | 说明 | 示例 |
|------|------|------|
| `API_BASE_URL` | FastAPI 后端地址 | `http://localhost:8000` |
| `API_KEY` | API 认证密钥 | `your-api-key-123` |

### 可选环境变量

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `ADMIN_HOST` | 绑定地址 | `0.0.0.0` |
| `ADMIN_PORT` | 监听端口 | `8501` |
| `ENV` | 运行环境 | `development` |
| `LOG_LEVEL` | 日志级别 | `INFO` |

### Streamlit 配置文件

配置文件位置：`backend/admin/.streamlit/config.toml`

```toml
[server]
port = 8501
address = "0.0.0.0"
headless = true

[theme]
primaryColor = "#FF4B4B"
backgroundColor = "#FFFFFF"
```

## 页面详解

### 统计仪表板

#### 统计卡片

显示关键指标：
- 总订单数
- 待支付/已支付/已交付/已过期/已取消数量
- 成功率 = 已交付 / 总数
- 支付率 = (已支付 + 已交付) / 总数

#### 订单状态分布

- **图表类型**：甜甜圈饼图
- **数据来源**：`GET /api/admin/stats/summary`
- **交互**：悬停显示数量和百分比

#### 订单类型分布

- **图表类型**：柱状图
- **分类**：Premium 会员、余额充值、TRX 兑换
- **交互**：柱状图上显示具体数值

#### 订单流转漏斗

- **阶段**：创建 → 支付 → 交付
- **数据**：显示每个阶段的数量和转化率
- **用途**：分析流失环节

### 订单管理

#### 订单列表

**表格字段**：
- 订单 ID
- 类型（Premium/充值/兑换）
- 金额（USDT，3 位小数）
- 状态（带颜色标识）
- 收件人
- 创建时间
- 支付时间

**过滤器**：
- 订单类型：全部、premium、deposit、trx_exchange
- 订单状态：全部、PENDING、PAID、DELIVERED、EXPIRED、CANCELLED
- 每页数量：10/20/50/100

**分页控制**：
- ⬅️ 上一页
- 当前页/总页数
- ➡️ 下一页

#### 订单详情

**信息展示**：
- 订单基本信息（ID、类型、金额、状态）
- 时间信息（创建、支付、交付）
- 收件人信息

**操作按钮**：
- ✅ 更新状态：选择新状态并更新
- 🔴 取消订单：输入取消原因并取消（仅待支付/已支付订单）

### 系统设置

#### API 配置

- **API Base URL**：显示当前配置的后端地址
- **API Key**：
  - 默认脱敏显示（前 8 位 + `***`）
  - 勾选"显示完整 API Key"查看完整值
  - 点击"📋 复制"复制完整 Key

#### 环境信息

显示关键环境变量：
- 环境（development/production）
- API Base URL
- API Key（脱敏）
- Log Level
- Log JSON Format

#### 关于

- 版本信息
- 功能模块列表
- 技术栈
- 开源地址

### 健康监控

#### 整体状态

- **状态指示器**：🟢 健康 / 🟡 降级 / 🔴 异常
- **指标卡片**：
  - 数据库延迟（ms）
  - Redis 延迟（ms）
  - Worker 状态

#### 组件详细检查

**数据库**：
- 连接状态
- 响应延迟
- 错误信息（如有）

**Redis**：
- 连接状态
- PING 延迟
- 错误信息（如有）

**Worker**：
- 活跃 Worker 数量
- 队列任务数量
- 错误信息（如有）

#### 自动刷新

- **开关**：勾选"🔄 自动刷新"
- **间隔**：5/10/30/60 秒
- **手动刷新**：点击"🔄 立即刷新"

## 故障排除

### 问题 1：无法启动管理界面

**症状**：
```
Error: API_BASE_URL is not set
```

**解决方案**：
1. 检查 `.env` 文件是否存在
2. 确认 `API_BASE_URL` 和 `API_KEY` 已配置
3. 重新加载环境变量

### 问题 2：API 请求失败

**症状**：
```
❌ 加载失败: API 请求失败: 401
```

**解决方案**：
1. 检查 FastAPI 后端是否已启动
2. 验证 API Key 是否正确
3. 确认 API Key 在后端 `API_KEYS` 配置中

### 问题 3：订单列表为空

**症状**：
```
📭 暂无订单数据
```

**可能原因**：
1. 数据库中确实没有订单
2. 过滤条件过于严格
3. API 返回数据格式错误

**解决方案**：
1. 清除所有过滤条件
2. 检查数据库是否有数据
3. 查看浏览器控制台日志

### 问题 4：健康检查失败

**症状**：
```
❌ 连接异常
```

**解决方案**：
1. 检查 FastAPI 后端是否运行
2. 验证数据库/Redis 服务是否启动
3. 检查网络连接

### 问题 5：自动刷新不工作

**症状**：勾选自动刷新后页面不刷新

**解决方案**：
1. 检查浏览器是否阻止了自动刷新
2. 尝试手动刷新
3. 清除浏览器缓存

## 使用技巧

### 1. 快速查找订单

在订单管理页面：
1. 使用过滤器缩小范围
2. 在"订单详情"输入框直接输入订单 ID
3. 点击"查看详情"

### 2. 批量导出订单

Streamlit 自带导出功能：
1. 在订单列表表格右上角
2. 点击"⋮"菜单
3. 选择"Download as CSV"

### 3. 监控关键指标

在统计仪表板：
1. 关注"成功率"和"支付率"
2. 查看订单流转漏斗，识别流失环节
3. 定期刷新数据

### 4. 实时监控健康状态

在健康监控页面：
1. 开启"自动刷新"
2. 设置刷新间隔为 10 秒
3. 保持页面打开，实时监控

### 5. 自定义主题

编辑 `backend/admin/.streamlit/config.toml`：

```toml
[theme]
primaryColor = "#FF4B4B"  # 主色调
backgroundColor = "#FFFFFF"  # 背景色
secondaryBackgroundColor = "#F0F2F6"  # 次背景色
textColor = "#262730"  # 文字颜色
```

## 部署建议

### Docker 部署

创建 `Dockerfile.admin`:

```dockerfile
FROM python:3.11-slim

WORKDIR /app

# 安装依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制代码
COPY backend/admin ./backend/admin
COPY .env .env

# 暴露端口
EXPOSE 8501

# 启动命令
CMD ["streamlit", "run", "backend/admin/app.py", \
     "--server.port=8501", \
     "--server.address=0.0.0.0", \
     "--server.headless=true"]
```

构建并运行：

```bash
docker build -f Dockerfile.admin -t tg-admin .
docker run -p 8501:8501 --env-file .env tg-admin
```

### Kubernetes 部署

创建 `k8s/admin-deployment.yaml`:

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-interface
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: admin
        image: your-image:latest
        ports:
        - containerPort: 8501
        env:
        - name: API_BASE_URL
          value: "http://api-service:8000"
        - name: API_KEY
          valueFrom:
            secretKeyRef:
              name: api-credentials
              key: api-key
---
apiVersion: v1
kind: Service
metadata:
  name: admin-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8501
  selector:
    app: admin-interface
```

### Nginx 反向代理

```nginx
server {
    listen 80;
    server_name admin.yourdomain.com;
    
    location / {
        proxy_pass http://localhost:8501;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## 安全建议

1. **生产环境**：
   - 使用 HTTPS（配置 SSL 证书）
   - 限制访问 IP（Nginx/防火墙）
   - 定期轮换 API Key

2. **认证**：
   - 考虑添加登录认证
   - 使用强密码策略
   - 启用多因素认证（MFA）

3. **监控**：
   - 记录所有操作日志
   - 设置异常告警
   - 定期审计访问记录

## 常见问题

**Q: 如何修改端口？**

A: 设置环境变量 `ADMIN_PORT=8080` 或使用 `--server.port` 参数。

**Q: 如何关闭 Streamlit？**

A: 在终端按 `Ctrl+C`。

**Q: 数据不刷新怎么办？**

A: 点击页面右上角"🔄 刷新"按钮或按 `R` 键。

**Q: 如何查看日志？**

A: Streamlit 日志输出到终端，可以重定向到文件：
```bash
streamlit run backend/admin/app.py 2>&1 | tee admin.log
```

**Q: 支持多用户吗？**

A: Streamlit 原生不支持多用户认证，建议使用 Nginx 添加 Basic Auth 或集成第三方认证。

## 更新日志

### v1.0.0 (2024-10-29)
- ✅ 统计仪表板
- ✅ 订单管理
- ✅ 系统设置
- ✅ 健康监控
- ✅ API 客户端封装
- ✅ 自动刷新功能

## 反馈与支持

- **GitHub Issues**: https://github.com/Jack123-UU/tg_dgn_bot/issues
- **文档**: https://github.com/Jack123-UU/tg_dgn_bot/docs
